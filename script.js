document.addEventListener('DOMContentLoaded', function () { const progressBar = document.getElementById('progress-bar'); const progressText = document.getElementById('progress-text'); const loadingScreen = document.getElementById('loading-screen'); const startButtonContainer = document.getElementById('start-button-container'); const startButton = document.getElementById('start-button'); const mainContent = document.getElementById('main-content'); let progress = 0; function loadProgress() { if (progress < 100) { progress++; progressBar.style.width = progress + '%'; progressText.textContent = progress + '%'; if (progress === 100) { setTimeout(() => { startButtonContainer.classList.add('visible'); }, 500); } } } const interval = setInterval(loadProgress, 50); startButton.addEventListener('click', function () { loadingScreen.style.opacity = 0; loadingScreen.style.pointerEvents = 'none'; setTimeout(function () { mainContent.style.display = 'block'; mainContent.style.opacity = 1; }, 500); }); });
